# Database Configuration (MongoDB Atlas)
# For production, use secure connection strings with SSL/TLS
MONGODB_URI=mongodb+srv://servandoytio:qDn2Se8cKbWaPCeN@merncluster.2veth.mongodb.net/queueing-system?appName=mernCluster

# Super Admin Configuration
# For production, use strong, unique passwords and consider environment-specific credentials
SUPER_ADMIN_EMAIL=superadmin
SUPER_ADMIN_PASSWORD=SuperAdmin123!

# JWT Secret
# For production, use a strong, randomly generated secret (minimum 32 characters)
JWT_SECRET=queueing@2025system

# Server Configuration
NODE_ENV=development
PORT=5000
SERVER_PORT=5000
HOST=localhost
SERVER_HOST=localhost

# Production Security Configuration
# For production deployment, ensure proper security measures:
# - Set NODE_ENV=production
# - Use HTTPS (HTTPS=true)
# - Configure proper firewall rules
# - Use secure database connections
# - Implement proper CORS restrictions
# - Use strong, unique secrets and passwords
HTTPS=false
SECURE_COOKIES=false
TRUST_PROXY=false

# CORS Configuration - Support both localhost and network access (will be auto-updated)
# For production, restrict to specific domains only
CLIENT_URL=http://localhost:5174,http://192.168.1.5:5174
ALLOWED_ORIGINS=http://localhost:5174,http://192.168.1.5:5174,http://localhost:3000,http://192.168.1.5:3000

# Frontend Configuration - Dynamic based on environment (will be auto-updated)
VITE_API_URL=http://192.168.1.5:5000
VITE_FRONTEND_HOST=192.168.1.5
VITE_FRONTEND_PORT=5174
VITE_BACKEND_HOST=192.168.1.5

# Super Admin Credentials for Frontend
VITE_SUPERADMIN_USERNAME=superadmin
VITE_SUPERADMIN_PASSWORD=SuperAdmin123!

# Rate Limiting - Enhanced Security Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Session Security
SESSION_SECRET=your-super-secure-session-secret-key-change-in-production
SESSION_MAX_AGE=3600000
COOKIE_SECURE=false
COOKIE_HTTP_ONLY=true
COOKIE_SAME_SITE=strict

# CORS Security
CORS_ENABLED=true
CORS_CREDENTIALS=true
CORS_MAX_AGE=3600

# Content Security Policy
CSP_ENABLED=true
CSP_REPORT_ONLY=false

# Additional Security Headers
X_FRAME_OPTIONS=DENY
X_CONTENT_TYPE_OPTIONS=nosniff
X_XSS_PROTECTION=1; mode=block
REFERRER_POLICY=strict-origin-when-cross-origin

SUPER_ADMIN_USERNAME=superadmin